stages:
- name: build
  steps:
  - publishImageConfig:
      dockerfilePath: ./Dockerfile
      buildContext: .
      tag: michael770/api:latest
      pushRemote: true
      registry: index.docker.io
timeout: 60
notification: {}
